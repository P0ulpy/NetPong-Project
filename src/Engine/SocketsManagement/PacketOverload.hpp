//
// Created by flo on 11/02/2022.
//

#pragma once

#include <SFML/Network/Packet.hpp>
#include "Client/PlayerSettings.hpp"
#include "../../Game/Controllers/NetworkCharacterController/NetworkCharacterController.hpp"

namespace sf
{
    /*sf::Packet& operator <<(sf::Packet& packet, const PlayerSettings& playerSettings);
    sf::Packet& operator >>(sf::Packet& packet, PlayerSettings& playerSettings);

    sf::Packet& operator <<(sf::Packet& packet, const Vector2i& vector);
    sf::Packet& operator >>(sf::Packet& packet, Vector2i& vector);

    sf::Packet& operator <<(sf::Packet& packet, const Vector2<float>& vector);
    sf::Packet& operator >>(sf::Packet& packet, Vector2<float>& vector);

    sf::Packet& operator <<(sf::Packet& packet, const PlayerState& playerState);
    sf::Packet& operator >>(sf::Packet& packet, PlayerState& playerState);*/
}